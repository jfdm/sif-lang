sif problem

logging <- Problem "Logging"

logs       <- Functional "Must capture infomation."
persitance <- Functional "Logs must be kept."
levels     <- Functional "Logging Levels"
impact     <- Performance "Minimal Disruption"
integrity  <- Reliability "Logs must not be modified"
secure     <- Functional "Logs must not be kept confidential"
leakage    <- Functional "Logs must not leak information"
config     <- Usability "Mechanism must be configurable."
